# API

The `build_data` function is the main entry point for the returns-based API.
It creates a [`Data`](data.md) object from a returns DataFrame and an optional benchmark.

::: jquantstats.api.build_data
