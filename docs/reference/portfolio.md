# Portfolio

The `Portfolio` class is the recommended entry point when you have price series
and position sizes.  It compiles the NAV curve from raw inputs and exposes the
full analytics suite via `.stats`, `.plots`, and `.report`.

::: jquantstats.analytics.portfolio.Portfolio
