# Stats

The `Stats` class provides a comprehensive set of financial metrics and statistics.
It is accessed via the `.stats` property on a `Data` object or a `Portfolio`.

::: jquantstats._stats.Stats
